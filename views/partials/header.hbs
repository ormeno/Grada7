<!-- Navigation-->
<nav class="navbar navbar-expand-lg navbar-light fixed-top py-3" id="mainNav">
    <div class="container px-4 px-lg-5">
        <a class="navbar-brand" href="#">Grada7   <i class="bi bi-dash"></i>         
                  {{#if paramAltaExists}}
                    <input type="hidden" id="url" value='{{url}}'>
                  {{else}}
                      {{#if paramApostarExists}}
                      <input type="hidden" id="url" value='{{url}}'>
                      {{else}}
                        {{#if paramCerrarExists}}
                          <input type="hidden" id="url" value='{{url}}'>
                        {{else}}
                          {{#if paramDistribuirExists}}
                          <input type="hidden" id="url" value='{{url}}'>
                          {{else}}
                            <input type="hidden" id="url" value=''>
                          {{/if}}
                        {{/if}}
                      {{/if}}
                  {{/if}}
                  <button id="enableEthereumButton" class="btn btn-warning text-white">Conectar Metamask</button>
                  <p id="accountSelected" class="bg-dark text-white"></p>

        </a>
    </div>
</nav>